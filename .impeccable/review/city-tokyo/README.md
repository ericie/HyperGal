# City of Signs — vertical Tokyo blocks, September 16, 2026

User choice: mostly vertical Tokyo — stacked businesses, external stairs and
rooftop structures. Twelve new procedural blocks replace the earlier architectural
vocabulary; three surreal blocks remain. Thirty-three legacy blocks stay archived.

- `before.js`: renderer before the Tokyo block pass.
- `after.js`: current renderer.
- `mobile.png`: seed 1907, 390 × 844 CSS px.
- `square.png`: seed 1907, 1254 × 1254 CSS px; thumbnail source.
- `desktop-seed-420.png`: seed 420, 1440 × 900 CSS px.
- `catalog.png`: active block catalog, full page at 1440px viewport width.

Checked 480 seeded layouts, all 48 catalog drawings, active Tokyo block use,
shared-court bounds, fan clearance, one striped vessel, regeneration and resize.
Live browser checks cover rendering, active and archived catalogs, and inputs.
The detector found one catalog font-size advisory; the link size was aligned
with the documented description role. No user acceptance is implied.

Architectural references are linked in the piece README. Photos were inspiration
only. All artwork and the thumbnail come from the procedural renderer.
