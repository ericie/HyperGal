# drift

A field of slowly drifting points on a low-contrast ground. Wraps at the
edges, leaves a faint trail. Vanilla canvas — no libraries, no assets, no
build step.

Open `index.html` directly in any browser.
