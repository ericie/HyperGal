# Elevator

A dead-front elevator that keeps an entirely routine schedule through places
which should not have elevator service.

The piece runs on its own. The floor buttons can interrupt the route, and the
small speaker button enables a synthesized motor, bell, and door sounds. It is
one self-contained HTML file with no external assets or dependencies.

Open `index.html` directly in a browser.
