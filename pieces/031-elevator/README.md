# Elevator

A dead-front elevator that keeps an entirely routine schedule through places
which should not have elevator service.

The piece runs on its own. The photographed floor buttons can interrupt the
route. The façade and moving door crops use one local photographic asset; five
static photographic office rooms wait beyond the doors. Nothing in the rooms
moves. There are no external dependencies.

Open `index.html` directly in a browser.
